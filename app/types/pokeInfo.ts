import type { PokeCard } from "./pokemon";

export interface PokeInfoRes extends PokeCard {
  qqq: string;
  // id:PokeCard['id'],
  // height:PokeCard['height'],
  // weight:PokeCard['weight'],
  // image:PokeCard['image'],
  // name:PokeCard['name'],
}
